
  <div class="media-body">
    <p>
    <div>
     <%= comment.body %>
   </div>
    <small>
       <%= image_tag(comment.user.avatar.tiny.url, class: 'img-circle') if comment.user.avatar? %>
       Submitted <%= time_ago_in_words(comment.created_at) %> ago by
       <%= comment.user.name %><br/>
     </small>
    </p>
  </div>
